using UnityEngine;
using System.Collections;
using System;

public class OnLoad : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log(Application.dataPath);
		// string csv = 
		// CSVReader.SplitCsvGrid(csv.text)
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
